.row_gallery_view_close
  = link_to gallery_view_photo_close_path do
    %i.fa-sharp.fa-solid.fa-circle-xmark
.row_gallery_view
  - if previous_image_in_gallery_present?(@photo.id)
    .col-md-1.previous_button
      = link_to galleries_previous_view_photo_path(id: @photo.id) do
        %i.fa-solid.fa-circle-arrow-left
  .col-md-10.photo_box
    = image_tag(@photo.path_to_image, alt: @photo.alt_to_image)
  - if next_image_in_gallery_present?(@photo.id)
    .col-md-1.next_button
      = link_to galleries_next_view_photo_path(id: @photo.id) do
        %i.fa-solid.fa-circle-arrow-right
