.pdp
  %section.pdp-hero
    %nav.pdp-crumbs
      = link_to 'Статті', admin_articles_path
      %span.sep /
      %span.cur= strip_tags(localized(@article.title)).truncate(60)
    %h1.pdp-title Редагування

  .pdp-block
    .adm-card
      - if flash[:message].present?
        -# own modifier instead of Bootstrap's .alert-danger, whose pale
        -# pink background made the light text unreadable on the dark theme
        %p.adm-flash{ class: ('adm-flash--error' if flash[:class].to_s.include?('danger')) }= flash[:message]
      = form_tag admin_article_path(@article.id), method: :put do
        = render 'new_edit'
        .adm-actions
          = submit_tag 'Save', class: 'btn btn--sm'
    = link_to admin_articles_path, class: 'ed-back' do
      %i.arr ←
      Назад до списку
