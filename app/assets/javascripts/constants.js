//Place all the behaviors and hooks related to the matching controller here.
//All this logic will automatically be available in application.js.
//You can use CoffeeScript in this file: http://coffeescript.org/



/*
function probuem(element_id)
{

    var obj = document.getElementsByClassName(element_id);
    if(obj == null)
    {
        alert("Элемент с id: " + element_id + " не найден!");
    }
    else
    {
        obj[0].style.color = "rgb(255, 255, 0)";
        alert("Элемент с id: " + element_id + " найден!");
        //document.getElementsByClassName("text_size").style.backgroundColor = rgb(255,0,0);
       //obj.style.color = "rgb(255, 0, 0)"; //Скрываем элемент



    }
}
*/
//$(document).ready(function(){
//    $('.text_size').click(function(){
//        var obj = document.getElementsByClassName(element_id);
//        $(this).parent().children('.hiden_element').toggle('normal');
//        return false;
//    });
//});

//$(document).ready(function(){
//    $('.spoiler_links').click(function(){
//        $(this).parent().children('.spoiler_body').toggle('normal');
//        return false;
//    });
//});